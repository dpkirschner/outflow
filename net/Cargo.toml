[package]
name = "outflow-net"
version = "0.1.0"
edition = "2021"
description = "Networked adapters for outflow (Plaid, secrets, Anthropic)"

[dependencies]
outflow-core = { path = "../core" }
ureq = "2"
serde_json = "1"
